# Changelog

## 0.0.1

- Initial Robot LSP VS Code client package.
- Adds Robot Framework language registration for `.robot` and `.resource` files.
- Starts the Python `robot_lsp` language server over stdio.
- Adds syntax highlighting, configuration bridge, Test Explorer discovery, run support, and no-debug Robot execution MVP.
